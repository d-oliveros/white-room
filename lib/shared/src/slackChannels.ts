export enum SlackChannel {
  Sandbox = 'slack-webhook-sandbox',
}

export enum SlackMessageBlockType {
  Header = 'header',
  Section = 'section',
  Image = 'image',
  Divider = 'divider',
}

export enum SlackMessageBlockTextType {
  PlainText = 'plain_text',
  Markdown = 'mrkdwn',
}
